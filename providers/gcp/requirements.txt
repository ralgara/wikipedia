# GCP pipeline runtime dependencies
# Lean subset of the root requirements.txt — no boto3, no jupyter, no plotly.

# Wikipedia API client
urllib3>=2.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Visualization (generate-report.py)
matplotlib>=3.7.0
seaborn>=0.12.0

# GCP storage
google-cloud-storage>=2.10.0

# scipy, PyWavelets, statsmodels are intentionally excluded —
# they're used by local analysis scripts only, not the pipeline.

# Spike narratives (shared/wikipedia/narratives.py). Undeclared until 2026-08-08, which is
# why every report had empty narrative boxes: batch_generate caught the ImportError and
# degraded to "Narrative unavailable" on every call.
anthropic>=0.40.0
